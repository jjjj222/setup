ln -s . ~/.vim
ln -s vimrc ~/.vimrc
ln -s vimrc_after ~/.vimrc_after

mkdir ~/.vim_tmp
mkdir ~/.vim_tmp/backup
mkdir ~/.vim_tmp/swp
mkdir ~/.vim_tmp/undo

mkdir bundle
git clone https://github.com/gmarik/Vundle.vim.git ~/.vim/bundle/Vundle.vim
:PluginInstall
