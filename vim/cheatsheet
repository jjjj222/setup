# Move
jump to last cursor position --> Ctrl-o (or `` or '')
jump to next cursor position --> Ctrl-i

# Tab Width
reset tap width              --> :set tabsop=4

# Turn off match highlight
turn off match highlight     --> :match

# Rotate pane
hsplit to vsplit             --> <C-W> H

# Plugin: "surround"
add "                        --> ysiw"
change " to <p>              --> cs"<p>
delete "                     --> ds"

# Open
open link                    --> gx
open file                    --> ,o
